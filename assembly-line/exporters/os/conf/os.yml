micro:
  platform-web:
    assembly-line:
      exporters:
        group-name: [go.micro.web.platform]
        collector:
          name: go.micro.srv.platform_collector
        cpu:
          enable: false
          interval: 5
        disk:
          enable: false
          interval: 5
          paths:
        docker:
          enable: false
          interval: 5
        host:
          enable: false
          interval: 5
        load:
          enable: false
          interval: 5
        mem:
          enable: true
          interval: 5
        net:
          enable: false
          interval: 5
          kinds:
        process:
          enable: false
          interval: 5